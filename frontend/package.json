{
  "name": "frontend-root",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["agent-ui"],
  "scripts": {
    "dev": "npm run dev --workspace agent-ui",
    "build": "npm run build --workspace agent-ui",
    "start": "npm run start --workspace agent-ui",
    "lint": "npm run lint --workspace agent-ui",
    "prettier": "npm run prettier --workspace agent-ui",
    "prettier:fix": "npm run prettier:fix --workspace agent-ui"
  }
}
